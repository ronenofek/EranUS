// ── Calendar ─────────────────────────────────────────────────────────────
// The calendar view is a static Google Calendar iframe.
// No runtime logic needed — showView('calendar') in app.js handles
// the overflow-y toggle so the iframe fills the viewport correctly.
const Calendar = {};
